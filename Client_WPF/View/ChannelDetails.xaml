<Window
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:Client_WPF_Converter="clr-namespace:Client_WPF.Converter" xmlns:Client_WPF_ViewModel="clr-namespace:Client_WPF.ViewModel" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:Client_WPF_Behavior="clr-namespace:Client_WPF.Behavior" mc:Ignorable="d"
	x:Class="Client_WPF.ChannelDetails"
	x:Name="Window"
	Title="{Binding WindowTitle, Mode=OneWay}"
	Width="640" Height="480">
	<Window.Resources>
		<Client_WPF_Converter:StringToHtmlPageConverter x:Key="StringToHtmlPageConverter"/>
		<Client_WPF_ViewModel:FeedDetailsViewModel x:Key="FeedDetailsViewModelDataSource" d:IsDataSource="True"/>
		<Thickness x:Key="Thickness">0,10,0,0</Thickness>
	</Window.Resources>
	<Window.DataContext>
		<Binding Mode="OneWay" Source="{StaticResource FeedDetailsViewModelDataSource}"/>
	</Window.DataContext>
	<Window.Background>
		<ImageBrush ImageSource="/Client_WPF;component/Images/background.jpg"/>
	</Window.Background>
	<Grid ScrollViewer.CanContentScroll="True" Margin="10" ScrollViewer.VerticalScrollBarVisibility="Auto" >
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<TextBlock TextWrapping="Wrap" Text="{Binding RootChannel.Description}" Foreground="{DynamicResource DefaultColor}" TextAlignment="Justify" VerticalAlignment="Top"/>
		<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" >
			<ItemsControl ItemsSource="{Binding Items}" Foreground="{DynamicResource DefaultColor}" ItemTemplate="{DynamicResource DataTemplate}" ScrollViewer.VerticalScrollBarVisibility="Disabled" VerticalContentAlignment="Stretch" Focusable="False">
				<ItemsControl.Resources>
					<DataTemplate x:Key="DataTemplate">
						<StackPanel Margin="0,10,0,0" ScrollViewer.VerticalScrollBarVisibility="Disabled">
							<TextBlock TextWrapping="Wrap" Text="{Binding Title}" TextAlignment="Center" LineHeight="18.667" FontSize="18.667" FontWeight="Bold"/>
							<WebBrowser Height="Auto" VerticalAlignment="Top" Margin="{DynamicResource Thickness}" ScrollViewer.VerticalScrollBarVisibility="Disabled">
								<i:Interaction.Behaviors>
									<Client_WPF_Behavior:WebBrowserUtilityBehavior HtmlContent="{Binding Description, Converter={StaticResource StringToHtmlPageConverter}}"/>
								</i:Interaction.Behaviors>
							</WebBrowser>
						</StackPanel>
					</DataTemplate>
				</ItemsControl.Resources>
			</ItemsControl>
		</ScrollViewer>
	</Grid>
</Window>