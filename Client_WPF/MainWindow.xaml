<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Client_WPF_ViewModel="clr-namespace:Client_WPF.ViewModel" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" mc:Ignorable="d" x:Class="Client_WPF.MainWindow"
        Title="MainWindow" Height="350" Width="525">
	<Window.Resources>
		<Client_WPF_ViewModel:LoginPageViewModel x:Key="LoginPageViewModelDataSource" d:IsDataSource="True"/>
	</Window.Resources>
    <Grid DataContext="{Binding Mode=OneWay, Source={StaticResource LoginPageViewModelDataSource}}">
    	<TextBox Height="24" Margin="112,80,253,0" TextWrapping="Wrap" Text="{Binding Username}" VerticalAlignment="Top"/>
    	<TextBox Height="24" Margin="112,120,253,0" TextWrapping="Wrap" Text="{Binding Password}" VerticalAlignment="Top"/>
    	<Button Height="24" Margin="112,0,253,120" VerticalAlignment="Bottom" Content="Login">
    		<i:Interaction.Triggers>
    			<i:EventTrigger EventName="Click">
    				<i:InvokeCommandAction Command="{Binding Login, Mode=OneWay}"/>
    			</i:EventTrigger>
    		</i:Interaction.Triggers>
    	</Button>
        
    </Grid>
</Window>
